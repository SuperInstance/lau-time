[package]
name = "lau-time"
version = "0.1.0"
edition = "2021"
description = "Time system for Lau — game time, real time, and time dilation based on room state"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
